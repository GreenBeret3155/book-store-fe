import { ComponentProperties } from './component-properties.model';

describe('ComponentProperties', () => {
  it('should create an instance', () => {
    expect(new ComponentProperties()).toBeTruthy();
  });
});
